#include<iostream>
#include<fstream>
#include<string>
#define 
using namespace std; 
int main() {
	fstream fi("Oxford-English-Dictionary.txt", ios::in); 
	string temp;
	string dictionary[]
	while (getline(fi, temp)) {
		if (temp.length() != 1) {

		}
	}


	return 0; 
}